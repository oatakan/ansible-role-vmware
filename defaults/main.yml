---

role_action: provision

vcenter:
  hostname: localhost
  username: Administrator@localhost
  password: password
  datacenter: mydatacenter
  cluster: mycluster
  resource_pool: ~

datastore_iso_folder: iso

ansible_port: 22
instance_wait_retry_limit: 300
instance_wait_connection_timeout: 400

enable_custom_attributes: yes
enable_tags: yes
vcenter_category_name: INFRA